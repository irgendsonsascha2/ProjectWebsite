# commit

Commit current workspace changes with a concise, sensible message.

This command is intended for standard agents to:
- inspect modified files
- summarize the change clearly
- stage the relevant files
- create a Git commit using a meaningful subject line and optional body

Example:
```bash
git add .
git commit -m "Fix .cursor commands for all agents"
```
